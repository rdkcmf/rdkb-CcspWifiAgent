[Unit]
Description=WiFi Telemetry STA Service

[Service]
Type=simple
EnvironmentFile=/etc/device.properties
WorkingDirectory=/usr/ccsp/wifi
ExecStart=/bin/sh -c '/usr/ccsp/wifi/stahealth_log.sh'
ExecStop=/bin/sh -c 'echo "`date`: Stopping/Restarting WiFi Telemetry STA" >> ${PROCESS_RESTART_LOG}'
Restart=always

StandardOutput=syslog

[Install]
WantedBy=wifi-telemetry.target
